---
import GoTo from "../../hyperlinks/GoTo/GoTo.astro";
import { NETWORKS } from "./networks";
---

<article>
  {
    NETWORKS.map(({ url, Icon, display }) => (
      <GoTo title={display} href={url}>
        {<Icon />}
      </GoTo>
    ))
  }
</article>

<style>
  article {
    display: flex;
    gap: var(--md);

    a {
      width: 32px;
      height: 32px;
      transition: transform ease 0.1s;

      &:hover {
        transform: scale(1.5);
      }

      svg {
        max-width: 100%;
        max-height: 100%;
      }
    }
  }
</style>
